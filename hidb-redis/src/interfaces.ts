export interface HiDBRedisInput {
  host?: string,
  port?: number,
  docId?: string,
  maxHistory?: number
}

export interface HiDBRedisSettings {
  host: string,
  port: number,
  docId: string,
  maxHistory: number
}
