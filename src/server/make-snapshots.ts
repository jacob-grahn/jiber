// await storage.setState(roomId, room.confirmedState)                         // store the new state
// await storage.removeActions(roomId, lastTimeMs)                             // remove the actions
