export interface JiberRedisInput {
  host?: string,
  port?: number,
  docId?: string,
  maxHistory?: number
}

export interface JiberRedisSettings {
  host: string,
  port: number,
  docId: string,
  maxHistory: number
}
