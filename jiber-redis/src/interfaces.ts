export interface JiberRedisInput {
  host?: string,
  port?: number,
  maxHistory?: number,
  docId?: string
}

export interface JiberRedisSettings {
  host: string,
  port: number,
  maxHistory: number,
  docId: string
}
