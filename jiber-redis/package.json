{
  "name": "jiber-redis",
  "version": "0.0.1",
  "description": "Redis middleware for Jiber",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "lint": "tslint --fix -p tsconfig.json",
    "test": "docker-compose up -d && jest src --ci --coverage --runInBand && docker-compose down"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "redis": "^2.8.0"
  },
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "@types/redis": "^2.8.12",
    "jest": "^24.6.0",
    "redis-commands": "^1.4.0",
    "ts-jest": "^24.0.1",
    "tslint": "^5.15.0",
    "tslint-config-standard": "^8.0.1",
    "typescript": "^3.4.1"
  }
}
